#pragma once


// ----------------------- RUN COMMAND FUNCTIONS -----------------------------
void runSorting(int a[], int n, char algo[], char mode[], double& runtime, long long& compare);
// function that runs the first command type:
void runCommandTypeOne(int argc, char algorithmName[], char fileName[], char outputParameters[]);

// functions that runs the second command type:
void runCommandTypeTwo(char algo[], int inputSize, char inputOrder[], char outputParameters[]);

// functions that runs the third command type:
void runCommandTypeThree(char algo[], int inputSize, char inputOrder[], char outputParameters[]);

// functions that runs the fourth command type:

// functions that runs the fifth command type:

